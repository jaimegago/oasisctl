version: "2"

run:
  timeout: 5m

formatters:
  enable:
    - gofmt
    - goimports
  settings:
    goimports:
      local-prefixes:
        - github.com/jaimegago/oasisctl

linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - ineffassign
    - unused
